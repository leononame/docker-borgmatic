location:
  source_directories:
    - /mnt/source/example
  repositories:
    - user@host.example.com:repo

storage:
  compression: lz4
  archive_name_format: "example-{now}"

retention:
  keep_hourly: 24
  keep_daily: 7
  keep_weekly: 4
  keep_monthly: 12
  keep_yearly: 10
  prefix: "example-"

consistency:
  checks:
    - repository
    - archives
  check_last: 10
  prefix: "example-"

hooks:
  on_error:
    - echo "Error while creating a example."
